# levcorpus reuses the backend package (app.*) for NLP / embeddings / CourtListener.
# Install the backend requirements too when running standalone:
#   pip install -r ../backend/requirements.txt
# Corpus-specific additions:
pandas==2.2.3
pyarrow==17.0.0
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
pgvector==0.3.3
pydantic==2.9.2
httpx==0.27.2
