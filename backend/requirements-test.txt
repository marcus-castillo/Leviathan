# Minimal deps to run the backend unit tests (tests/test_bias.py).
# These tests are pure compute — no torch / spaCy / Postgres needed. Full runtime
# deps live in requirements.txt.
pytest==8.3.3
pydantic==2.9.2
pydantic-settings==2.5.2
numpy==1.26.4
scipy==1.14.1
statsmodels==0.14.3
# Imported transitively at test-collection time (app.bias package __init__ -> app.models -> app.db).
# create_engine imports the psycopg DBAPI module, but does NOT open a connection, so no running
# database is required to collect/run the tests.
sqlalchemy==2.0.35
pgvector==0.3.3
psycopg[binary]==3.2.2
